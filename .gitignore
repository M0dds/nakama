# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Wrangler local state + dev secrets (.dev.vars holds the TMDB token for
# `wrangler dev`; the prod token is a Worker secret, never committed)
.wrangler/
.dev.vars
.dev.vars.*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Supabase CLI link state + transient schema dumps (the real security
# snapshot is supabase/migrations/00000000000000_baseline.sql)
supabase/.temp/
supabase/_live_schema.sql
